"""Fixture builders: `sessions` writes a Claude Code Session log, `repos`
builds a scratch git repo. Importable as `tests.support.<module>` — the
conftest fixtures are thin wrappers over these, and a test that needs a shape
the fixtures do not offer should import the builder directly.
"""
